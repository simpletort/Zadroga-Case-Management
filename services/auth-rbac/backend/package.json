{
  "name": "legal-portal-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest firestore/firestore.rules.test.js",
    "test:watch": "jest firestore/firestore.rules.test.js --watchAll",
    "test:emulator": "firebase emulators:exec --only firestore \"jest firestore/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase":                     "^10.12.0",
    "jest":                         "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout":     30000,
    "verbose":         true
  }
}
