# api/requirements.txt
# Dependencies for the Cloud Functions API layer.
# Cloud Build installs these before deploying functions.
#
# These mirror functions/requirements.txt — both must stay in sync.

firebase-functions==0.4.2
firebase-admin==6.5.0
google-cloud-firestore==2.16.1
flask==3.0.3
requests==2.32.3
sendgrid==6.11.0
