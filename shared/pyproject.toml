[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:BuildBackend"

[project]
name = "simpletort-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "starlette>=0.36",
    "google-auth>=2.28",
    "google-cloud-firestore>=2.14",
    "cachetools>=5.3",
]
